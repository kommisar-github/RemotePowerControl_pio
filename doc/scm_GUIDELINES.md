# /scm — Agent Guidelines
**Last Updated:** 2026-06-05

## Abstract

**TL;DR:** Durable, project-specific guidelines for the `/scm` agent
(Source Control). This file is the agent's **only** sanctioned write target for
notes that should survive across sessions. The agent appends here **only
when PM or the user explicitly asks** — never as a side effect of doing
work. Examples of project-specific commit conventions, branch policies, or PR-template requirements live here when the user formalises them.

**Load when:** the `/scm` agent starts a session, or when PM is
auditing roster consistency via `/pm audit`.

**Key facts:**
- Owner: `/scm` (Primary), `/pm` (Secondary) — per DOC_OWNERSHIP_MATRIX.md
- Write trigger: explicit PM/user request only. Cite the request verbatim in the commit message.
- All other auto-memory writes by specialists are forbidden (see SKILL.md `## Memory Policy`).

**Owner:** `/scm` (Primary per DOC_OWNERSHIP_MATRIX.md)
**Related:** `.claude/skills/scm/SKILL.md`, `DOC_OWNERSHIP_MATRIX.md`

---

## Conventions

(none yet — populate only when PM or the user asks)

## Decisions

(none yet — append dated entries when PM or the user asks)

## Open Questions

(none yet)
